[pytest]
# Put the project root on sys.path so `import src...` / `import Ammeters...` work.
pythonpath = .
testpaths = tests
addopts = -v
markers =
    integration: tests that start the emulator socket servers (slower)
